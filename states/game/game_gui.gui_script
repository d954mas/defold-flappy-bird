local hash_set_score = hash("set_score")

function init(self)
	self.score_lbl=gui.get_node("score_label")
end

function on_message(self, message_id, message, sender)
	if(message_id == hash_set_score) then
		gui.set_text(self.score_lbl,message.score)
	end	
end

